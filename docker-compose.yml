version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: outventure-web
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/html/
